import { defineConfig } from "vite";

export default defineConfig({
    base: "/mortgage-repayment-calculator-vite/", // Substitua pelo nome do seu repositório no GitHub
    build: {
        outDir: "docs", // Define a pasta de saída como 'docs'
    },
});
